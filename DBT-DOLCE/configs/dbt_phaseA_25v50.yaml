# GENERATED by scripts/make_phase_a_configs.py from dbt_25deg.yaml.
# Phase-A probe geometry: 25p50d, symmetric arc.
# Only data/geometry/operator/results paths differ from the base;
# model/training/sampling sections are inherited verbatim.
data:
  raw_dir: dataset/compressed
  processed_dir: dataset/processed_phaseA_25v50
  split_file: dataset/split_phaseA.json
geometry:
  leap_cfg: configs/leap_dbt_phaseA_25v50.cfg
  angle_range: 50.0
  num_projections: 25
  sirt_iterations: 200
  image_size: 512
  pixel_size_mm: 0.273
attenuation:
  air: 0.0
  adipose: 0.41
  fibroglandular: 0.78
  skin: 0.78
split:
  train_ratio: 0.8
  val_ratio: 0.1
  test_ratio: 0.1
  seed: 42
model:
  pretrained_ckpt: model_zoo/model512_all.pt
  image_size: 512
  num_channels: 128
  num_res_blocks: 2
  num_heads: 4
  num_head_channels: 64
  attention_resolutions: 32,16,8
  resblock_updown: true
  dropout: 0.0
  use_fp16: true
  diffusion_steps: 1800
  noise_schedule: linear
  weighted_condition: false
  use_condition: rls
  data_range: '-11'
training:
  batch_size: 2
  num_workers: 4
  lr: 2.0e-05
  weight_decay: 0.0
  ema_rate: 0.9999
  log_interval: 100
  val_interval: 5000
  val_batch_size: 8
  max_val_batches: 50
  max_steps: 100000
  resume_checkpoint: ''
  auto_resume: true
  output_dir: runs/dbt_25deg_full
sampling:
  sampler: ddpm
  ddim_steps: 100
  eta: 0.0
  dc_mode: prox
  operator_npz: runs/operator/A_phaseA_25v50_512.npz
  gamma_start: 1.0
  gamma_end: 1.0
  pinv_rcond: 1.0e-12
  prox_solver: cgrad
  rho_start: 1.0
  rho_end: 0.1
evaluation:
  results_dir: results/dbt_phaseA_25v50
